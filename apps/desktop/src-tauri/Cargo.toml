[package]
name = "minify"
version = "0.1.0"
description = "Orion"
authors = ["you"]
edition = "2021"
default-run = "minify"

[lib]
name = "minify_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "minify"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
tauri = { version = "2.9.3", features = ["macos-private-api"] }
tauri-plugin-opener = "2.5"
tauri-plugin-fs = "2.4"
tauri-plugin-process = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "6.0"
tokio = { version = "1", features = ["full"] }
axum = { version = "0.8", features = ["http1", "json"] }
reqwest = { version = "0.12", features = ["json", "stream"] }
futures-util = "0.3"
tempfile = "3"
sha2 = "0.10"
sha1 = "0.10"
base64 = "0.22"
url = "2.5"
chrono = "0.4"
rand = "0.9"
lazy_static = "1.4"
hex = "0.4"
urlencoding = "2.1"
webbrowser = "1.0"
discord-rich-presence = "1.0"
log = "0.4"

[target.'cfg(target_os = "linux")'.dependencies]
chacha20poly1305 = "0.10"

[target.'cfg(not(target_os = "linux"))'.dependencies]
keyring = "2"

# Tints the native title bar through DWM, and drives the taskbar thumbnail
# buttons, which are COM (`ITaskbarList3`) and need `windows`' wrappers. 0.61
# is the version Tauri already pulls in.
[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = ["Win32_Foundation", "Win32_Graphics_Dwm"] }
windows = { version = "0.61", features = [
  "Win32_Foundation",
  "Win32_Graphics_Gdi",
  "Win32_System_Com",
  "Win32_System_Registry",
  "Win32_UI_Shell",
  "Win32_UI_WindowsAndMessaging",
] }

# The self-updater is desktop-only (no mobile targets are built).
[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-updater = "2"
